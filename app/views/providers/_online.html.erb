<% providers = OnlineProvider.all %>


<% providers.each do |provider| %>

    <ul class='<%= "pricing_column gradient_blue" %>'>

        <li class="pricing_header1"><%=provider.film_provider.name %></li>
        <li class="pricing_header2"><%= link_to image_tag("core/logos/#{provider.film_provider.image_uri}"), provider.film_provider.uri %><%#= render partial:"by_post_limited_data", locals:{disc:provider.one} %> </li>

        <li class="odd" data-table="PPV"><%= provider.price %></li>
        <li class="even" data-table="Subscription"><%= provider.type %></li>
        <li class="even" data-table="Trail Period"><%= provider.film_provider.trial_period %></li>
        <li class="odd" data-table="Templates"><span class="pricing_no"></span></li>

        <li class="pricing_footer"><a href='<%=provider.film_provider.uri%>' class="pricing_button">Sign Up </a></li>
        
    </ul><!-- END FIRST CONTENT COLUMN -->
<% end %>


<!-- <div class="header">
    <h3 data-toggle="collapse" data-target="#providerOnline">Online</h3>
    <h4>Sites that offer unlimited viewing online for a monthly fee, or charge per single viewing</h4>
</div>
<div id="providerOnline" class="collapse in">
    <table class="table">
        <thead>
            <tr>
                <th>Provider</th>
                <th>Subscription</th>
                <th>*Pay Per View</th>
                <th>Trial Period</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
        <% providers.each do |provider| %>
            <tr>
                <td><%= link_to image_tag("core/logos/#{provider.film_provider.image_uri}"), provider.film_provider.uri %>
                </td>
                <td></td>
                <td></td>
                <td><%= provider.film_provider.trial_period %></td>
                <td> <%= link_to 'Sign up', provider.film_provider.uri, class:'button-link' %> </td>
            </tr>
        <% end %>
        </tbody>
        <tfoot>
            <tr>
               <td colspan="5">* Prices are an average cost for a new top title release in normal definition</td>
            </tr>
        </tfoot>
    </table>
</div>
 -->